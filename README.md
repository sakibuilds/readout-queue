# Readout Queue

Browser-only spoken-output operations tool. Paste multiple generic script segments once, then get a render queue with filenames, duration estimates, slates, pause hints, and copy/export actions.

No backend, no database, no real seed names or organizations. Built for preparing repeat voice-generation batches without manually renaming and reformatting each clip.
